const AUTH = {
    authorize: '/oauth/authorize',
    token: '/oauth/token',
    revoke: '/oauth/revoke',
}

const HOME = {
    TIMELINES: {
        home: '/api/v1/timelines/home',
    },
}

export const ENDPOINTS = {
    AUTH,
    HOME,
}
